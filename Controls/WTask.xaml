<UserControl x:Class="toDo.WTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:toDo" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="571" d:DesignWidth="871">
    <StackPanel Orientation="Horizontal">
        <CheckBox IsChecked="{Binding IsSelected}"/>
        <StackPanel>
            <TextBlock Text="{Binding Title}"  FontSize="15" FontFamily="Georgia"  FontWeight="Black" Margin="0, 0, 20, 6"/>
            <TextBlock Text="{Binding Description}"  FontSize="14" FontFamily="Georgia"  FontWeight="Black" Margin="0, 0, 20, 6"/>
            <TextBlock Text="{Binding createDateTime}" FontWeight="Black" Margin="0, 0, 20, 6"/>
        </StackPanel>
    </StackPanel>
</UserControl>
